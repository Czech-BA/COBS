error_INVALID_AUTHORIZATION_REDIRECT_URI_400:
  title: error_INVALID_AUTHORIZATION_REDIRECT_URI_400
  description: |
    The value of redirect uri is not valid (not registered)
  type: object
  required:
    - error
  properties:
    error:
      type: string
      example: "INVALID_AUTHORIZATION_REDIRECT_URI"
    parameters:
      $ref: "errorParameters.yaml#/errorParameters"
    scope:
      $ref: "scope.yaml#/scope"
    message:
      $ref: "errorMessage.yaml#/errorMessage"
  example:
    [{
      "error":"INVALID_AUTHORIZATION_REDIRECT_URI"
    }]