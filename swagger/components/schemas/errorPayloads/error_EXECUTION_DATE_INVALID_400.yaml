error_EXECUTION_DATE_INVALID_400:
  title: error_EXECUTION_DATE_INVALID_400
  description: |
    [EXECUTION_DATE_INVALID] –  Non validity execution date or execution mode (missing element  "validToDate" with combination mode UNTIL_DATE)
  type: object
  required:
    - error
  properties:
    error:
      type: string
      example: "EXECUTION_DATE_INVALID"
    parameters:
      $ref: "errorParameters.yaml#/errorParameters"
    scope:
      $ref: "scope.yaml#/scope"
    message:
      $ref: "errorMessage.yaml#/errorMessage"
  example:
    [{
      "error":"EXECUTION_DATE_INVALID",
      "scope": null,
      "parameters": null,
      "message": null
    }]