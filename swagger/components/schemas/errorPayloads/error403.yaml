error403:
  title: error403
  description: |
    Authentication with an invalid certificate or expired access token, a call that does not match a third-party license.
  type: object
  properties:
    errors:
      type: array
      items:
        anyOf:
          - $ref: "error_FORBIDDEN_403.yaml#/error_FORBIDDEN_403"