requestNewDirectDebit:
  title: requestNewDirectDebit
  description: |
    Payload of query forn new direct debit
  type: object
  required:
    - limit
    - directDebit
    - debtorAccount
    - creditorAccount
  properties:
    limit:
      $ref: "../objects.yaml#/directDebitLimit"
    directDebit:
      $ref: "../objects.yaml#/directDebit"
    debtorAccount:
      $ref: "../objects.yaml#/directDebitDebtorAccount"
    creditorAccount:
      $ref: "../objects.yaml#/directDebitCreditorAccount"
    remittanceInformation:
      $ref: "../objects.yaml#/remittanceInformation"
