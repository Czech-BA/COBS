putPaymentAuthorizationInitiation:
  title: putPaymentAuthorizationInitiation
  description: Response message for put Payment authorization initiation.
  type: object
  required:
    - state
    - pollInterval
  properties:
    state:
      $ref: '../elements.yaml#/state'
    pollInterval:
      $ref: '../elements.yaml#/pollInterval'