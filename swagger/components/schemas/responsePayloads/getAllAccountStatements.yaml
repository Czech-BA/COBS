getAllAccountStatements:
  title: getAllAccountStatements
  description: Response message list of client payment account statements
  type: object
  required:
    - pageNumber
    - pageCount
    - pageSize
    - accounts
  properties:
    pageNumber:
      $ref: "../elements.yaml#/pageNumber"
    pageCount:
      $ref: "../elements.yaml#/pageCount"
    nextPage:
      $ref: "../elements.yaml#/nextPage"
    pageSize:
      $ref: "../elements.yaml#/pageSize"
    totalCount:
      $ref: "../elements.yaml#/totalCount"
    accountStatements:
      $ref: "../objects.yaml#/accountStatementsList"