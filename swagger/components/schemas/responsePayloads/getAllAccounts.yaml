getAllAccounts:
  title: getAllAccounts
  description: Response message list of client payment accounts
  type: object
  required:
    - pageNumber
    - pageCount
    - pageSize
    - accounts
  properties:
    pageNumber:
      $ref: "../elements.yaml#/pageNumber"
    pageCount:
      $ref: "../elements.yaml#/pageCount"
    nextPage:
      $ref: "../elements.yaml#/nextPage"
    pageSize:
      $ref: "../elements.yaml#/pageSize"
    totalCount:
      $ref: "../elements.yaml#/totalCount"
    accounts:
      $ref: "../objects.yaml#/accountsList"